<div class="container">
  <h1>Students</h1>
  <ol>
    <% @school_classes.each do |school_class| %>
    <li><%= link_to(school_class.title, school_class_path(school_class)) %></li>
    <% end %>
  </ol>
</div>
